import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GameWebProject',
  webDir: 'build',
  bundledWebRuntime: false,
  plugins: {}
};

export default config;
